# Implementation Plans

Generated by the improve skill on 2026-08-20 against `51f9e20`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add TableOfContents characterization tests | P1 | M | — | TODO |
| 002  | Fix observer lifecycle, retries, and hash apply | P1 | S | 001 | TODO |
| 003  | Gate auto-fetch plugin; errors; stale TOC; slim payload | P1 | S | — | TODO |
| 004  | Fix README v2 snippet + remaining docs drift | P1 | S | — | TODO |
| 005  | Include test:types in npm run check | P2 | S | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Dependency notes

- 002 requires 001 because observer tests must exist before changing retry/hash/unmount behavior.
- 003 may edit `TableOfContents.vue` at the same time as 002 — do not run those two executors in parallel on one worktree.
- 004 and 005 are independent.

## Findings considered and rejected

- Treat auto-fetch `path`/`collection` as a security vulnerability: Content owns those query APIs; this is the documented helper contract.
- Unify playground CSS: dual apps exist because Content majors cannot share `node_modules`.
- Husky/lint-staged, `@types/node` 20 pin, ESLint stylistic off, drop CI pack job: low leverage.
- New heading-level filter API: tree `depth` is documented; fix wording (plan 004) instead.
- Split `TableOfContents.vue` now: only after 001+002.
