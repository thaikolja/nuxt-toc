# npm is the project package manager (see package.json "packageManager").
registry=https://registry.npmjs.org/
engine-strict=false
# Nuxt / Vite / ESLint peer ranges often conflict across majors; install like Bun did.
legacy-peer-deps=true
