# Development setup

```bash
bun install
bun run dev:prepare
bun run dev:v3   # or dev:v2
bun run test
bun run prepack
```

Use Bun (`packageManager` field). Do not commit `package-lock.json`.

Brand asset: repository root `logo.png` (also under playground `public/` and `docs/public/`).
